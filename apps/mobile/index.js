/**
 * EmuZ Mobile App Entry Point
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './src/App';

// Register with 'main' which is the default expected by Expo/React Native
AppRegistry.registerComponent('main', () => App);
